{% comment %}
  Promotional banner section for special offers and deals
{% endcomment %}

<style>
  .promo-banner {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 1.5rem 0;
    position: relative;
    overflow: hidden;
  }

  .promo-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="dots" width="10" height="10" patternUnits="userSpaceOnUse"><circle cx="5" cy="5" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23dots)"/></svg>');
  }

  .promo-banner-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 1;
  }

  .promo-banner-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .promo-banner-text {
    flex: 1;
  }

  .promo-banner-text h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 0.5rem;
  }

  .promo-banner-text p {
    font-size: 1rem;
    margin: 0;
    opacity: 0.9;
  }

  .promo-banner-cta {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .promo-banner-btn {
    padding: 0.75rem 1.5rem;
    background: white;
    color: #667eea;
    text-decoration: none;
    border-radius: 50px;
    font-weight: 600;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .promo-banner-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
  }

  .promo-banner-timer {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .promo-banner-timer-item {
    text-align: center;
    min-width: 50px;
  }

  .promo-banner-timer-number {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    line-height: 1;
  }

  .promo-banner-timer-label {
    display: block;
    font-size: 0.8rem;
    opacity: 0.8;
    text-transform: uppercase;
  }

  .promo-banner-close {
    background: none;
    border: none;
    color: white;
    cursor: pointer;
    padding: 0.5rem;
    opacity: 0.7;
    transition: opacity 0.3s ease;
  }

  .promo-banner-close:hover {
    opacity: 1;
  }

  @media (max-width: 768px) {
    .promo-banner-content {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .promo-banner-text h3 {
      font-size: 1.2rem;
    }

    .promo-banner-cta {
      flex-direction: column;
      width: 100%;
    }

    .promo-banner-timer {
      justify-content: center;
    }
  }

  .promo-banner.hidden {
    display: none;
  }
</style>

<section class="promo-banner" id="promo-banner" {{ section.shopify_attributes }}>
  <div class="promo-banner-container">
    <div class="promo-banner-content">
      <div class="promo-banner-text">
        {% if section.settings.heading != blank %}
          <h3>{{ section.settings.heading }}</h3>
        {% endif %}
        
        {% if section.settings.description != blank %}
          <p>{{ section.settings.description }}</p>
        {% endif %}
      </div>

      <div class="promo-banner-cta">
        {% if section.settings.show_timer %}
          <div class="promo-banner-timer">
            <div class="promo-banner-timer-item">
              <span class="promo-banner-timer-number" id="timer-days">00</span>
              <span class="promo-banner-timer-label">Days</span>
            </div>
            <div class="promo-banner-timer-item">
              <span class="promo-banner-timer-number" id="timer-hours">00</span>
              <span class="promo-banner-timer-label">Hours</span>
            </div>
            <div class="promo-banner-timer-item">
              <span class="promo-banner-timer-number" id="timer-minutes">00</span>
              <span class="promo-banner-timer-label">Minutes</span>
            </div>
            <div class="promo-banner-timer-item">
              <span class="promo-banner-timer-number" id="timer-seconds">00</span>
              <span class="promo-banner-timer-label">Seconds</span>
            </div>
          </div>
        {% endif %}

        {% if section.settings.button_text != blank and section.settings.button_url != blank %}
          <a href="{{ section.settings.button_url }}" class="promo-banner-btn">
            {{ section.settings.button_text }}
          </a>
        {% endif %}
      </div>

      {% if section.settings.show_close_button %}
        <button class="promo-banner-close" onclick="closeBanner()" aria-label="Close banner">
          <span class="material-symbols-outlined">close</span>
        </button>
      {% endif %}
    </div>
  </div>
</section>

<script>
  {% if section.settings.show_timer and section.settings.end_date != blank %}
    // Countdown timer
    const endDate = new Date("{{ section.settings.end_date }}").getTime();
    
    function updateTimer() {
      const now = new Date().getTime();
      const distance = endDate - now;

      if (distance < 0) {
        document.getElementById("promo-banner").style.display = "none";
        return;
      }

      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);

      document.getElementById("timer-days").textContent = days.toString().padStart(2, '0');
      document.getElementById("timer-hours").textContent = hours.toString().padStart(2, '0');
      document.getElementById("timer-minutes").textContent = minutes.toString().padStart(2, '0');
      document.getElementById("timer-seconds").textContent = seconds.toString().padStart(2, '0');
    }

    updateTimer();
    setInterval(updateTimer, 1000);
  {% endif %}

  function closeBanner() {
    document.getElementById("promo-banner").classList.add("hidden");
    localStorage.setItem("promoBannerClosed", "true");
  }

  // Check if banner was previously closed
  if (localStorage.getItem("promoBannerClosed") === "true") {
    document.getElementById("promo-banner").classList.add("hidden");
  }
</script>

{% schema %}
{
  "name": "Promo Banner",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "🔥 Special Offer - Up to 50% Off Electronics!"
    },
    {
      "type": "text",
      "id": "description",
      "label": "Description",
      "default": "Limited time deal on smartphones, laptops, and accessories"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    },
    {
      "type": "checkbox",
      "id": "show_timer",
      "label": "Show countdown timer",
      "default": true
    },
    {
      "type": "text",
      "id": "end_date",
      "label": "End Date (YYYY-MM-DD HH:MM:SS)",
      "info": "Format: 2024-12-31 23:59:59"
    },
    {
      "type": "checkbox",
      "id": "show_close_button",
      "label": "Show close button",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Promo Banner",
      "settings": {
        "heading": "🔥 Special Offer - Up to 50% Off Electronics!",
        "description": "Limited time deal on smartphones, laptops, and accessories",
        "button_text": "Shop Now",
        "button_url": "/collections/sale",
        "show_timer": true,
        "end_date": "2024-12-31 23:59:59",
        "show_close_button": true
      }
    }
  ]
}
{% endschema %}
